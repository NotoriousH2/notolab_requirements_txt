# unsloth 실습 전용 환경.
# transformers/trl 버전이 메인 환경과 어긋나므로 별도 venv(/tmp/.venv-unsloth)에 설치한다.

# PyTorch CUDA 13.0 빌드 지정
# 메인 환경과 같은 torch를 명시적으로 고정해, uv 캐시에서 하드링크로 공유되게 한다.
--index-url https://download.pytorch.org/whl/cu130
torch==2.11.0
torchvision==0.26.0
torchaudio==2.11.0

# 나머지 패키지들은 PyPI에서 설치
--extra-index-url https://pypi.org/simple

# unsloth (transformers/trl을 자체 버전으로 끌어온다 — 핀하지 않는다)
unsloth
unsloth-zoo

# 학습 유틸리티
trl
peft
accelerate
datasets
bitsandbytes
huggingface_hub
hf_transfer

# 지원 패키지
setuptools<82.0
dotenv
pandas

# ipython
ipykernel
ipywidgets
